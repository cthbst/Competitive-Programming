#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> 
    for (int cs = 1, u, v; cin >> u >> v; cs++) {
        if (u == -1 && v == -1) break;
        edges.push_back({u, v});
    }
    return 0;
}
